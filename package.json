{
  "name": "stepts",
  "module": "lib/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "validate-step": "uv run scripts/validate_step.py",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
